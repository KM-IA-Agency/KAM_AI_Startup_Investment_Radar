# PDF Export

Generate Markdown report:

```bash
python src/report.py
```

Export latest report to PDF:

```bash
python src/export_pdf.py
```

Output folder:

```text
reports/generated/
```

Current limitation: tables are rendered as plain text in the first MVP version.
